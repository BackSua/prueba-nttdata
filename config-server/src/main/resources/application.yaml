server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: develop
          uri: https://github.com/BackSua/prueba-nttdata
          search-paths: config-data
  application:
    name: config-service
